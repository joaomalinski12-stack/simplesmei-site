# dependencies
node_modules

# build output
dist
dist-ssr
.vite

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
.idea

# env
.env
.env.*
!.env.example

# mcp — config do Ubersuggest MCP (conexão via OAuth na conta; nunca commitar)
.mcp.json

# claude code — só as settings pessoais (rules/ e skills/ SÃO versionados)
.claude/settings.local.json
